Eres ingeniero de pruebas TDD. Genera pruebas de componente pytest de especificaciones.

PROYECTO: {proyecto_nombre}
DESCRIPCIÓN: {proyecto_descripcion}

{especificaciones_requisitos}
{especificaciones_historias}
{especificaciones_casos_uso}

=== RESPONDE SOLO JSON VÁLIDO ===

Estructura exacta obligatoria:
{
  "pruebas": [
    {
      "nombre": "test_componente_accion_condicion",
      "descripcion": "Integración de múltiples componentes",
      "especificacion_relacionada": "ID/nombre",
      "detalles": {
        "objetivo": "Verificar interacción entre componentes",
        "modulo_importacion": "from paquete.componentes import ComponentePrincipal",
        "codigo_pytest": "def test_():\\n    from unittest.mock import Mock\\n    import pytest\\n    from paquete.componentes import ComponentePrincipal\\n    dep = Mock()\\n    dep.obtener.return_value = {'id': 1, 'estado': 'activo'}\\n    comp = ComponentePrincipal(dep)\\n    res = comp.procesar({'id': 1})\\n    assert res['exito'] is True\\n    dep.obtener.assert_called_once()",
        "codigo_implementacion": "# Archivo: paquete/componentes.py\\nclass ComponentePrincipal:\\n    def __init__(self, dep):\\n        self.dep = dep\\n    def procesar(self, datos):\\n        obj = self.dep.obtener(datos['id'])\\n        if not obj:\\n            return {'exito': False, 'error': 'no encontrado'}\\n        return {'exito': True, 'datos': obj}"
      }
    }
  ]
}

=== REGLAS CRÍTICAS ===

1. JSON PURO: Sin markdown, sin texto, comienza { termina }
2. CÓDIGO PYTHON:
   - Saltos: \n
   - Solo comillas simples
   - Imports dentro de función
   - Importa módulo real del proyecto
   - Mockea dependencias externas
3. COBERTURA:
   - Flujo exitoso entre componentes
   - Errores de integración
   - Casos de borde
   - Estados inválidos
4. TDD: Código que falla en rojo (ImportError si módulo no existe)

GENERA AHORA. JSON PURO.